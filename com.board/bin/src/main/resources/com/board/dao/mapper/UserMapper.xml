<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper  namespace="com.board.dao.mapper.UserMapper">
<!-- <resultMap type="user" id="userMap">
		<id property="userId" column="user_id"/>
		<result property="userPw" column="user_pw"/>
		<result property="userName" column="user_name"/>
		<result property="email" column="email"/>
		<result property="phone" column="phone"/>
		<result property="addNo" column="add_no"/>
		<result property="addD" column="add_d"/>
		<result property="gender" column="gender"/>
	</resultMap> -->
	<select id="getUser" parameterType="string" resultType="user">
		select user_id userId,user_pw userPw,user_name userName,email,phone,add_no addNo,add_d addD,gender
		from users
		where user_id =#{value}
	</select>
	
	<insert id="addUser" parameterType="user">
		insert into users(user_id,user_pw,user_name,email,phone,add_no,add_d,gender) values( #{userId,jdbcType=VARCHAR},#{userPw,jdbcType=VARCHAR},#{userName,jdbcType=VARCHAR},#{email,jdbcType=VARCHAR},
		 		#{phone,jdbcType=VARCHAR},#{addNo},#{addD,jdbcType=VARCHAR},#{gender})
	</insert>
	
	 <update id="updateUser" parameterType="user">
		update users set user_id=#{userId},user_pw=#{userPw},user_name=#{userName}
		,email=#{email},phone=#{phone},add_no=#{addNo},add_d=#{addD},gender=#{gender}
		where user_id=#{userId}
	</update>
	
	<delete id="delUser" parameterType="user">
		delete users
		where user_id=#{userId}
	</delete> 
</mapper>